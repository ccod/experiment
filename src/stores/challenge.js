const defaultState = {selectPlayer: null}
const reducer = (state = defaultState, { type, paylaod }) => {
    switch(type) {
        case foo:
            return foobar
        default: return state
    }
}

export { reducer }